/*let add = require('./te')
//console.log(add.phones('i',2))


let fs = require('fs');

// Read the file 'wr.js' in the same directory
fs.readFile('training1/wr.js', 'utf-8', function(err, data) {
    if (err) {
        console.error('Error reading file:', err.message); // Log detailed error message
        console.error('Error code:', err.code); // Log the error code for more detail
        console.error('Error stack:', err.stack); // Log the stack trace to understand where the error occurred
    } else {
        console.log("File content:", data); // Log file content if read successfully
    }
});

fs.writeFile('training1/wr.txt', 'AAAAAAAAAAAAHHHHHHHHH', function(err, data) {
    if (err) {
        console.error('Error reading file:', err.message); // Log detailed error message
        console.error('Error code:', err.code); // Log the error code for more detail
        console.error('Error stack:', err.stack); // Log the stack trace to understand where the error occurred
    } else {
        console.log(
            "File content:", data); // Log file content if read successfully
    }
});

fs.appendFile('training1/wr.txt', '\n hmm', function(err, data) {
    if (err) {
        console.error('Error reading file:', err.message); // Log detailed error message
        console.error('Error code:', err.code); // Log the error code for more detail
        console.error('Error stack:', err.stack); // Log the stack trace to understand where the error occurred
    } else {
        console.log(
            "File content:", data); // Log file content if read successfully
    }
});*/

/*fs.unlink('training1/wr.txt',function(){
    console.log('file deleted'
    */

    function a (){
        return 11;
    }
    exports.a = a